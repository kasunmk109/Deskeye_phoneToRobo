{
  "name": "deskeye-phone-to-robo",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://kasunmk109.github.io/Deskeye_phoneToRobo",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && gh-pages -d dist",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  }
}
